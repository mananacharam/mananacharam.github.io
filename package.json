{
  "name": "voter-search-app",
  "version": "1.0.0",
  "description": "Voter search application with dashboard",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:github": "vite build --base=/",
    "preview": "vite preview",
    "serve": "vite preview",
    "deploy": "npm run build:github && gh-pages -d dist",
    "lint": "echo 'Linting not configured'"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "bootstrap": "^5.3.2",
    "@fortawesome/fontawesome-free": "^6.4.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.0",
    "vite": "^4.5.0",
    "gh-pages": "^6.3.0"
  }
}